---
title: "Deployment Flow"
sidebar_position: 1
description: "How approved intent becomes deployed policy."
---

import DeploymentFlowDiagram from '@site/src/components/diagrams/DeploymentFlowDiagram';

# Deployment Flow

The developer experience should feel like normal configuration as code.

1. A team opens a GitHub PR with connectivity intent.
2. GitHub Actions validates schema and source context.
3. The destination resolver maps FQDN to destination identity.
4. Metadata enrichment adds ownership, environment, tenant, compliance, asset, and cloud context.
5. The GitHub Action calls the managed Beacon PDP API.
6. The Beacon PDP evaluates OPA policy and returns an allow/deny verdict.
7. Denies show up in the PR with rule IDs and remediation.
8. Allows generate primary-control artifacts with verdict IDs stamped on them.
9. GitOps applies Kubernetes and mesh artifacts.
10. Terraform Enterprise applies cloud/provider-managed controls.
11. Sentinel blocks plans that bypass approved modules or missing verdict metadata.
12. Gatekeeper blocks unmanaged cluster resources.
13. Runtime collectors and assurance start checking drift.

<DeploymentFlowDiagram />

The dedicated PR integration details live in [GitHub Action Verdict](./github-action-verdict.md).
